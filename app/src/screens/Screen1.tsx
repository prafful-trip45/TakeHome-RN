import { ScreenScaffold } from './ScreenScaffold';

/** Screen 1 — light status bar: light band + dark icons. */
export function Screen1() {
  return <ScreenScaffold title="Screen 1 (OTA)" statusBarColor="#f5f5f7" statusBarStyle="dark" />;
}
