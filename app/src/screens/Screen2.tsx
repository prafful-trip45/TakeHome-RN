import { ScreenScaffold } from './ScreenScaffold';

/** Screen 2 — DARK status bar: dark band + light icons (task 9). Body is shared. */
export function Screen2() {
  return <ScreenScaffold title="Screen 2" statusBarColor="#0b0b0f" statusBarStyle="light" />;
}
